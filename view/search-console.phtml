<?php

use App\Application\Devflow;
use App\Shared\Services\Registry;

use function App\Shared\Helpers\admin_url;
use function App\Shared\Helpers\help_block;
use function Qubus\Security\Helpers\esc_attr;
use function Qubus\Security\Helpers\esc_html;
use function Qubus\Security\Helpers\esc_html__;
use function Qubus\Security\Helpers\t__;

$this->parent('framework::backend/layouts/admin-layout');
$this->block('backend', function ($param) {

Registry::getInstance()->{'set'}('screen_parent', 'simple-seo');
Registry::getInstance()->{'set'}('screen_child', 'simple-seo');
$r = Registry::getInstance()->get('simple-seo');
?>

<div class="content-wrapper">
    <div class="box box-solid">
        <div class="box-header with-border">
            <i class="fa fa-chart-line"></i>
            <h3 class="box-title"><?= esc_html__('Search Statistics', $r['id']); ?></h3>

            <div class="pull-right">
                <button type="button" class="btn btn-danger" onclick="window.location='<?= admin_url('plugin/simple-seo/');?>'"><i class="fa fa-magnifying-glass"></i> <?=esc_html__('Seo Settings', $r['id']);?></button>
                <button type="button" class="btn btn-warning" onclick="window.location='<?= admin_url('plugin/simple-seo/redirects/');?>'"><i class="fa fa-diamond-turn-right"></i> <?=esc_html__('Redirects', $r['id']);?></button>
                <button type="button" class="btn btn-success" onclick="window.location='<?= admin_url('plugin/simple-seo/indexing/');?>'"><i class="fa fa-indent"></i> <?=esc_html__('Indexing', $r['id']);?></button>
                <button type="button" class="btn btn-dark" onclick="window.location='<?= admin_url('plugin/simple-seo/404-monitor/');?>'"><i class="fa fa-binoculars"></i> <?=esc_html__('404 Monitor', $r['id']);?></button>
            </div>
        </div>
    </div>

    <section class="content">
        <?= Devflow::$PHP->flash->display(); ?>
        <?php if (!empty($param['error'])): ?>
            <div class="alert alert-danger">
                <?= esc_html($param['error']); ?>
            </div>
        <?php endif; ?>

        <?php if (empty($param['isConnected'])): ?>
            <?=help_block(
                '',
                t__('Google Search Console is not connected.
                Add your Google Client ID and Client Secret in SEO Settings, then connect your account.', $r['id'])
            );?>

            <a href="<?= esc_attr(admin_url('plugin/simple-seo/')); ?>" class="btn btn-primary">
                <?=esc_html__('Go to SEO Settings', $r['id']);?>
            </a>
        <?php elseif (empty($param['settings']['google_search_console_site_url'])): ?>
            <?=help_block(
                '',
                t__('Google is connected, but no Search Console Site URL has been configured.', $r['id'])
            );?>

            <a href="<?= esc_attr(admin_url('plugin/simple-seo/')); ?>" class="btn btn-warning">
                <?=esc_html__('Configure Site URL', $r['id']);?>
            </a>
        <?php else: ?>

            <div class="row">
                <div class="col-sm-3">
                    <div class="small-box bg-aqua">
                        <div class="inner">
                            <h3><?= esc_html((string) $param['stats']['clicks']); ?></h3>
                            <p><?=esc_html__('Clicks', $r['id']);?></p>
                        </div>
                    </div>
                </div>

                <div class="col-sm-3">
                    <div class="small-box bg-green">
                        <div class="inner">
                            <h3><?= esc_html((string) $param['stats']['impressions']); ?></h3>
                            <p><?=esc_html__('Impressions', $r['id']);?></p>
                        </div>
                    </div>
                </div>

                <div class="col-sm-3">
                    <div class="small-box bg-yellow">
                        <div class="inner">
                            <h3><?= esc_html((string) $param['stats']['ctr']); ?></h3>
                            <p>CTR</p>
                        </div>
                    </div>
                </div>

                <div class="col-sm-3">
                    <div class="small-box bg-red">
                        <div class="inner">
                            <h3><?= esc_html((string) $param['stats']['position']); ?></h3>
                            <p><?=esc_html__('Avg Position', $r['id']);?></p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="box box-primary">
                <div class="box-header with-border">
                    <h3 class="box-title"><?=esc_html__('Google Search Console', $r['id']);?></h3>
                </div>

                <div class="box-body">
                    <p>
                        <?=esc_html__('Connected property:', $r['id']);?>
                        <code><?= esc_html((string) $param['settings']['google_search_console_site_url']); ?></code>
                    </p>

                    <button
                            type="button"
                            class="btn btn-default"
                            id="devflow-seo-test-gsc"
                            data-url="<?= esc_attr(admin_url('plugin/simple-seo/search-console/test/')); ?>"
                    >
                        <?=esc_html__('Test Connection', $r['id']);?>
                    </button>

                    <div id="devflow-seo-gsc-result" style="margin-top:15px;"></div>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6">
                    <div class="box box-default">
                        <div class="box-header with-border">
                            <h3 class="box-title"><?=esc_html__('Top Queries', $r['id']);?></h3>
                        </div>

                        <div class="box-body table-responsive no-padding">
                            <table class="table table-striped">
                                <thead>
                                <tr>
                                    <th><?=esc_html__('Query', $r['id']);?></th>
                                    <th><?=esc_html__('Clicks', $r['id']);?></th>
                                    <th><?=esc_html__('Impressions', $r['id']);?></th>
                                    <th>CTR</th>
                                    <th><?=esc_html__('Position', $r['id']);?></th>
                                </tr>
                                </thead>
                                <tbody>
                                <?php if (empty($param['topQueries'])): ?>
                                    <tr>
                                        <td colspan="5"><?=esc_html__('No query data available.', $r['id']);?></td>
                                    </tr>
                                <?php endif; ?>

                                <?php foreach ($param['topQueries'] as $row): ?>
                                    <tr>
                                        <td><?= esc_html((string) ($row['keys'][0] ?? '')); ?></td>
                                        <td><?= esc_html((string) ($row['clicks'] ?? 0)); ?></td>
                                        <td><?= esc_html((string) ($row['impressions'] ?? 0)); ?></td>
                                        <td><?= esc_html(isset($row['ctr']) ? round(((float) $row['ctr']) * 100, 2) . '%' : '0%'); ?></td>
                                        <td><?= esc_html(isset($row['position']) ? round((float) $row['position'], 1) : '—'); ?></td>
                                    </tr>
                                <?php endforeach; ?>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="box box-default">
                        <div class="box-header with-border">
                            <h3 class="box-title"><?=esc_html__('Top Pages', $r['id']);?></h3>
                        </div>

                        <div class="box-body table-responsive no-padding">
                            <table class="table table-striped">
                                <thead>
                                <tr>
                                    <th><?=esc_html__('Page', $r['id']);?></th>
                                    <th><?=esc_html__('Clicks', $r['id']);?></th>
                                    <th><?=esc_html__('Impressions', $r['id']);?></th>
                                    <th>CTR</th>
                                    <th><?=esc_html__('Position', $r['id']);?></th>
                                </tr>
                                </thead>
                                <tbody>
                                <?php if (empty($param['topPages'])): ?>
                                    <tr>
                                        <td colspan="5"><?=esc_html__('No page data available.', $r['id']);?></td>
                                    </tr>
                                <?php endif; ?>

                                <?php foreach ($param['topPages'] as $row): ?>
                                    <tr>
                                        <td>
                                            <a href="<?= esc_attr((string) ($row['keys'][0] ?? '#')); ?>" target="_blank">
                                                <?= esc_html((string) ($row['keys'][0] ?? '')); ?>
                                            </a>
                                        </td>
                                        <td><?= esc_html((string) ($row['clicks'] ?? 0)); ?></td>
                                        <td><?= esc_html((string) ($row['impressions'] ?? 0)); ?></td>
                                        <td><?= esc_html(isset($row['ctr']) ? round(((float) $row['ctr']) * 100, 2) . '%' : '0%'); ?></td>
                                        <td><?= esc_html(isset($row['position']) ? round((float) $row['position'], 1) : '—'); ?></td>
                                    </tr>
                                <?php endforeach; ?>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>

        <?php endif; ?>
    </section>
</div>

<?php }); ?>
